# www server with http, don't redirect since we don't have ssl cert yet
server {
  listen 80;
  server_name quizz.virouleau.fr;
  root /home/btest/btest/client/build;
}
server {
  listen 80;
  server_name quizz-api.virouleau.fr;
  root /home/btest/btest/server/public;
}
